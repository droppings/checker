<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
    <title>checker</title>
    <style>
    	ul{
    		list-style-type: none;
    	}
    </style>
</head>
<body>
    <h1>CHECK</h1>
    
    <h2>代码规范</h2>
    <ol>
    <% let spe = data.specification; %>
    <% for(let item in spe){ %>
		<li>
			<h3><%= spe[item].desc %></h3>
			<ul>
				<% for(let i = 0, len = spe[item].data.length; i < len; i++){%>
					<li>
						<pre><%= spe[item].data[i]%></pre>
					</li>
				<%}%>
			</ul>
		</li>
    <%}%>
    </ol>
</body>
</html>